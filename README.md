# Expense Tracker

A simple CLI expense tracker built with Python and JSON storage.

## Features

- Add expenses (amount, category, description)
- View all expenses with total spending
- View spending grouped by category
- Delete expenses by ID
- Auto-saves to `transactions.json`

## Requirements

- Python 3.6+
- No external dependencies

## Usage

```bash
python main.py
```
